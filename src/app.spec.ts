import 'mocha'
import { expect } from 'chai'

describe('Your Bolt App', () => {
    describe('A section of tests', () => {
        it('returns true', () => {
            expect(true).to.eql(true);
        })
    })
});